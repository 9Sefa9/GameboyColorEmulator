class Opcode{
    constructor(instruction, parameters, opcode, cycles, func){
        this.instruction = instruction;
        this.parameters = parameters;
        this.opcode = opcode;
        this.cycles = cycles;
    }

}